#!/bin/bash
# Git alias to get the previous Nth revision of a file. Like a HEAD~N but for a file instead of a branch

num_version_rollback=$1
file=$2

git log --pretty=format:'%h' $file | awk -v row="$num_version_rollback" 'FNR == row+1 {print}'
