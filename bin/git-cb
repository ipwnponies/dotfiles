#! /bin/bash
# Usage:
# git cb <branch>
# Switch to local branch if it exists. If not, create a branch that tracks origin/master.

if git rev-parse "$1" &>/dev/null; then
    git checkout "$1"
else
    git checkout origin/master -b "$1"
fi
