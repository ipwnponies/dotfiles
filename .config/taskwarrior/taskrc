search.case.sensitive=no
alias.burndown=burndown.daily
default.command=ready limit:10
report.next.columns=id,start.age,entry.age,depends,priority,project,tags,recur,scheduled.remaining,due.relative,until.remaining,description,urgency
report.ready.columns=id,start.age,entry.age,depends.indicator,priority,project,tags,recur.indicator,scheduled.countdown,due.remaining,until.remaining,description,urgency

urgency.uda.priority..coefficient=1.8
urgency.uda.priority.L.coefficient=0
urgency.age.max=30
urgency.age.coefficient=10
